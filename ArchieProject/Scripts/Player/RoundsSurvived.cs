using System.Collections;
using UnityEngine;
using UnityEngine.UI;

public class RoundsSurvived : MonoBehaviour //This is the text that counts up to the amount of rounds survived in the game over screen
{
    //public Text roundsText;

    //private void OnEnable()
    //{
    //    StartCoroutine(AnimateText());
    //}

    //IEnumerator AnimateText()
    //{
    //    int round = 0; //starting round
    //    roundsText.text = "0"; //Making sure that the text starts at zero

    //    yield return new WaitForSeconds(.7f); //adding a delay for our fade in animation

    //    while (round < PlayerStats.waves)
    //    {
    //        round++;
    //        roundsText.text = round.ToString();

    //        yield return new WaitForSeconds(.05f); //adding small delay so we can actually see it
    //    }
    //}

}
