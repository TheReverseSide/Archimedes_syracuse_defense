using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlialBuildPoint : MonoBehaviour
{

    //Should serve as the point where all glials are spawned
    //At the beginning of the round, simply spawn normal myelin glials
    GameObject[] buildPoints;

    private void Start()
    {
        // buildPoints = GameObject.Find
    }


}
